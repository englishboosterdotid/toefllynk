version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: toefllynk-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: passwordku
      POSTGRES_DB: toefllynk
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: toefllynk-app
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://postgres:passwordku@db:5432/toefllynk
      NODE_ENV: production
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
